root: "."
batch_size: 8
instances_per_identity: 4
num_workers: 4
epochs: 30
early_stopping_patience: 8
early_stopping_min_delta: 0.001
lr: 1e-4
weight_decay: 1e-4
temperature: 0.1
triplet_margin: 0.03
projection_dim: 512
metric_loss_weight: 1.0
id_loss_weight: 0.5
freeze_backbone: true
unfreeze_last_backbone_block: false
# mode: "untrained"
mode: "train"
checkpoint_path: null
image_size: 288
checkpoint_dir: "checkpoints_simclr"
embedding_checkpoint_path: "artifacts/embedding_checkpoints/embedding_backbone.pt"
max_samples: null
